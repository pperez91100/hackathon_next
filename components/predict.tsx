import React from "react";

const Predict: React.FC = () => {

    return (
        <div className="flex flex-col items-center justify-center">
            <h1 className="text-4xl font-bold text-gray-800">Predict JO</h1>
            <p className="mt-2 text-center text-lg font-medium text-gray-600">Predict the next Olympic Games</p>
        </div>

    );
}

export default Predict;
